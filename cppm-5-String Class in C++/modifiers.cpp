#include <bits/stdc++.h>
using namespace std;

int main()
{
    // string a = "Hello";
    // string b = "A";
    // a = a + b;
    // a = a + " " + b;
    // a+=b;
    // a.append(b);
    // a = a + "A";
    // a.push_back('A');
    // a.pop_back();
    // a.pop_back();
    // a = "Go";
    // a.assign("See");
    string a = "HelloWorld";
    // a.erase(4);
    // a.erase(4, 1);
    // a.replace(4, 1, "aaa");
    // a.replace(4, 0, "aaa");
    a.insert(5, " Sohel's ");
    cout << a << endl;
    return 0;
}